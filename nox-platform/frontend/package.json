{
    "name": "nox-platform-frontend",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "turbo run dev",
        "build": "turbo run build",
        "lint": "turbo run lint"
    },
    "devDependencies": {
        "turbo": "^2.3.0",
        "prettier": "^3.1.0"
    },
    "packageManager": "pnpm@9.0.0"
}